#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/sh" ]; then
  "$basedir/sh"  "$basedir/../n/bin/n" "$@"
  ret=$?
else 
  sh  "$basedir/../n/bin/n" "$@"
  ret=$?
fi
exit $ret
